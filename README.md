## The 'ls' from linux coreutils

Made with C++20  
supports only the -l flag  
colors are kinda off  
width of printed fileds is not adaptive

build via cmake
```
 $ mkdir build && cd $_  
 $ cmake ..
 $ cmake --build .
 ```

